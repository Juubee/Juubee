controller.on('bot_space_join', function (bot, message) {
  bot.reply(message, "Hi, i'm the Fun Fact bot"
  , function (err, newMessage) {
      if (newMessage.roomType == "group") {
        bot.reply(message, "<br/>_Note that this is a 'Group' Space.
        }
     });
 });
